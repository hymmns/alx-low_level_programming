#include "main.h"

/**
 * _strchr - dummy function.
 * @s: char pointer
 * @c: char
 *
 * Return: char pointer.
 */
char *_strchr(char *s, char c)
{
	if (*s == 'c' || c == 'c')
		return (c);

	return (s);
}
